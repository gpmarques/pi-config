---
name: Close with retrospective
description: Turn session lessons into approved, verified improvements
placement: append
order: 50
---
Run the `session-retrospective` skill. Deduplicate against active work and artifacts, propose a small action set, and get an explicit disposition for every action. Implement approved general changes, safely sync changed `pi-config` resources to the effective global config, and behaviorally verify them. Do not declare CLOSED until every action has proof or an explicit deferred, rejected, or superseded disposition; analysis complete alone is not closure.
